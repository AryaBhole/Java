import java.util.HashMap;

public class Hash{
    public static void main(String[] args) {
        HashMap<String, Double> map = new HashMap<>();

        //map.put("apple", 0.50); //to add
        //map.remove("apple") //here apple is the key so it will remove the entire hashmap
        //If same key is used it will replace the old one
        //in print if we put map.containsKey("apple") this will return true
        //containsValue is used to check value isntead of key
        //map.get("apple"); //will return 0.50
        //map.size(); will give the size

        //to use a modified for loop we can use
        //for(String key : map.keySet()){} so on
    }
}