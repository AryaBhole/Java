import java.util.ArrayList;

public class ArrayL{
    public static void main(String[] args) {
        ArrayList<Integer> list = new ArrayList<>();

        //list.add();
        //lsit.remove(index);
        //list.set(index, thing);
        //list.get(index);
        //list.size() //returns total size of the array list(number of objects in it)
        //seems similar to lists in python
        //Collections.sort(list); //sorts in alphabetical order (This will import the
        //collections util on top)

        //can use scanner and other stuff as well to add to this list
    }
}